pipeline {
    agent any
    stages {
   
       stage("build") {
       
          steps {
              echo 'this is build.  Now it has changed'
              echo 'final change'
          }
       }
       stage("deploy") {
       
          steps {
              echo 'this is deploy'
          }
       }
    }
}
