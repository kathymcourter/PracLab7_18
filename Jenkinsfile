pipeline {
    agent any
    stages {
   
       stage("build") {
       
          steps {
              echo 'this is build.  Now it has changed'
          
          }
       }
       stage("deploy") {
       
          steps {
              echo 'this is deploy'
          }
       }
    }
}
